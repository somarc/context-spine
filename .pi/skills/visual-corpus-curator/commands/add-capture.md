---
description: Add a normalized capture to a visual corpus and generate the corresponding capture page
---
Use `visual-corpus-curator` to create a new immutable capture for: $@

1. Determine corpus scope and capture granularity.
2. Collect the source evidence.
3. Create the normalized capture manifest using `references/capture-schema.md`.
4. Regenerate the capture page using `visual-explainer`.
5. Report the manifest path and generated HTML path.
